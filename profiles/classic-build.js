
// This is the classic build, as you find it in the build directory.
// It pulls in allmost all of EMbedJS' features (kitchensink) and
// the global feature, that makes the embed object available in
// global scope.

require(['../profiles/kitchensink', 'feature!global']);